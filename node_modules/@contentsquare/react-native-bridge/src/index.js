import { CSMask } from './CSMask';
import { CSReliableTarget } from './CSReliableTarget';
import { CSWebView } from './CSWebView';
import { ErrorAnalysis } from './ErrorAnalysis';
import { Currency } from './constants';
import { handleUrl, start } from './core';
import {
  getCurrentSessionReplayLink,
  setDefaultMasking,
} from './sessionReplay';
import {
  resumeTracking,
  send,
  sendDynamicVar,
  sendTransaction,
  sendTransactionWithStringCurrency,
  stopTracking,
} from './tracking';
import { forgetMe, getUserId, optIn, optOut, sendUserIdentifier } from './user';

/**
 * Contentsquare
 * exports public functions
 */
export default {
  start,
  handleUrl,
  getCurrentSessionReplayLink,
  getUserId,
  optIn,
  optOut,
  forgetMe,
  resumeTracking,
  stopTracking,
  send,
  sendTransaction,
  sendTransactionWithStringCurrency,
  sendDynamicVar,
  setDefaultMasking,
  sendUserIdentifier,
};

/**
 * CSMask, CSReliableTarget, CSWebView, Currency, ErrorAnalysis
 * @exports CSMask
 * @exports CSWebView
 * @exports CSReliableTarget
 * @exports Currency
 * @exports ErrorAnalysis
 */
export { CSMask, CSReliableTarget, CSWebView, Currency, ErrorAnalysis };
