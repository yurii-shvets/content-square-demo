"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.removeWebViewInjection = exports.injectWebView = void 0;
var _nativeModules = require("./nativeModules");
/**
 * Injects the Contentsquare SDK in the native WebView found at the reference.
 *
 * @param {number} webViewTag the nativeTag of the WebView - from which we will extract the React Tag.
 */
var injectWebView = exports.injectWebView = function injectWebView(webViewTag) {
  _nativeModules.ContentsquareModule.injectWebView(webViewTag);
};

/**
 * Removes the Contentsquare SDK injection in the native WebView found at the reference.
 *
 * @param {number} webViewTag the nativeTag of the WebView - from which we will extract the React Tag.
 */
var removeWebViewInjection = exports.removeWebViewInjection = function removeWebViewInjection(webViewTag) {
  _nativeModules.ContentsquareModule.removeWebViewInjection(webViewTag);
};